<Page x:Class="Contingent.View.Workspaces.Service.Pages.DataReinstatement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:Contingent.ViewModel.Workspaces.Service.DataReinstatement"
      xmlns:local="clr-namespace:Contingent.View.Workspaces.Service.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="DataReinstatement">
    <Page.DataContext>
        <vm:DataReinstatementViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button
            Grid.Row="0"
            Content="Do it"
            Command="{Binding Path=ReinstatementRunCommand}"/>
        <TextBox
            Grid.Row="1"
            Style="{StaticResource LogTextBox}"
            Text="{Binding Path=ConsoleText, IsAsync=True, Mode=OneWay}"/>
    </Grid>
</Page>

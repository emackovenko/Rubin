<UserControl x:Class="University.Themes.Controls.EntityCheckBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:University.Themes.Controls"
             mc:Ignorable="d"
             x:Name="parent"
             VerticalAlignment="Center"
             HorizontalAlignment="Left">
    <StackPanel
        Orientation="Horizontal"
        Margin="5">
        <CheckBox
            x:Name="check"
            VerticalAlignment="Center"
            IsThreeState="False"
            IsChecked="{Binding ElementName=parent, 
                Path=Member, ValidatesOnDataErrors=True, 
                ValidatesOnExceptions=True, Mode=TwoWay, 
                UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock
            Margin="0,0,3,0"
            VerticalAlignment="Center"
            Text="{Binding ElementName=parent, Path=Header, StringFormat={}{0}:, Mode=OneWay}"/>
    </StackPanel>
</UserControl>
